########################################
# AI Proxy - Example Environment File  #
########################################

# --- Core Proxy Configuration ---

# Domain used for HTTPS (Traefik/Let's Encrypt)
DOMAIN=your-domain.com
# Optional separate domain for logs UI (logs.your-logs-domain.com, logs-api.your-logs-domain.com)
# If not set, uses DOMAIN for logs UI as well
# BASE_DOMAIN=your-logs-domain.com
# Email for Let's Encrypt registration
ACME_EMAIL=your-email@example.com

# Optional custom ports (defaults: 80/443)
# HTTP_PORT=80
# HTTPS_PORT=443

# Docker container user/group IDs (auto-detected in production)
# Defaults to 9999:9999 to expose permission issues during development
# HOST_UID=9999
# HOST_GID=9999

# Proxy API keys (comma-separated)
API_KEYS=your-secret-key-1,your-secret-key-2

# Upstream provider API keys (leave blank if not used)
OPENROUTER_API_KEY=
GEMINI_API_KEY=

# Global environment (affects Logs UI docs gating)
ENVIRONMENT=development

# Application logging configuration
LOG_LEVEL=INFO
ENABLE_FILE_LOGGING=true

# Configuration file path (optional, defaults to config.yml)
# CONFIG_PATH=config.yml

# Advanced Gemini adapter settings (optional)
# GEMINI_AS_IS=false


# --- Log Storage System (SQLite) ---

# Enable the offline log database tooling
LOGDB_ENABLED=true
# Enable full-text search
LOGDB_FTS_ENABLED=true
# Enable heuristic dialog grouping
LOGDB_GROUPING_ENABLED=true
# Partition granularity: daily|weekly
LOGDB_PARTITION_GRANULARITY=daily
# Concurrency and memory caps
LOGDB_IMPORT_PARALLELISM=2
LOGDB_MEMORY_CAP_MB=256
# Include raw text logs in bundles (optional)
LOGDB_BUNDLE_INCLUDE_RAW=false


# --- Logs UI API (Stage U1–U2) ---

# UI API keys (user role)
LOGUI_API_KEYS=logs-ui-user-key-1
# Admin UI API keys (admin role). If empty, admin endpoints are disabled
LOGUI_ADMIN_API_KEYS=logs-ui-admin-key-1

# CORS allowed origins (CSV). Use specific origins in production
LOGUI_ALLOWED_ORIGINS=https://logs.your-domain.com,http://localhost:5173,http://logsьф.localhost:51999

# Feature flags and limits
LOGUI_ENABLE_TEXT_LOGS=false
LOGUI_SSE_HEARTBEAT_MS=15000
LOGUI_RATE_LIMIT_RPS=10

# Root directory for SQLite partitions used by UI API
LOGUI_DB_ROOT=./logs/db

# When true, delete daily source partitions after successful weekly/monthly merges
# performed by `logdb auto` or programmatic merges. Default: false
LOGDB_CLEANUP_AFTER_MERGE=false
